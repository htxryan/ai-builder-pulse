name: Daily Newsletter

on:
  schedule:
    - cron: "7 6 * * *"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Set to 1 to skip publish + commit"
        required: false
        default: "0"

concurrency:
  group: daily-publish
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      DRY_RUN: ${{ github.event.inputs.dry_run || '0' }}
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      BUTTONDOWN_API_KEY: ${{ secrets.BUTTONDOWN_API_KEY }}
      REDDIT_CLIENT_ID: ${{ secrets.REDDIT_CLIENT_ID }}
      REDDIT_CLIENT_SECRET: ${{ secrets.REDDIT_CLIENT_SECRET }}
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          # Archivist needs write access; fetch full history so a follow-up
          # push is a plain fast-forward (no rebase needed against remote).
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      - run: pnpm test
      - name: Run orchestrator
        id: orchestrator
        run: pnpm start
      # E6: commit issues/{runDate}/ artifacts locally. If nothing was
      # written (DRY_RUN, skip statuses), the step records that and exits 0.
      # Commit happens BEFORE gitleaks so the scan sees the exact ref that
      # will be pushed.
      - name: Commit daily archive
        id: commit
        if: ${{ env.DRY_RUN != '1' }}
        run: |
          set -euo pipefail
          git config user.name "ai-builder-pulse-bot"
          git config user.email "ai-builder-pulse-bot@users.noreply.github.com"
          git add issues/ || true
          if git diff --cached --quiet; then
            echo "::notice::no archive changes to commit (S-02 skip, S-03 skip, or failed run)"
            echo "committed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          RUN_DATE="$(date -u +%Y-%m-%d)"
          git commit -m "archive: daily issue $RUN_DATE"
          echo "committed=true" >> "$GITHUB_OUTPUT"
      # Un-07 gitleaks scan between commit and push. A positive scan
      # (secret found in committed content) fails the run before anything
      # leaves the runner.
      - name: gitleaks secret scan
        if: ${{ steps.commit.outputs.committed == 'true' }}
        # Pinned to v2 commit SHA (floating tags can be moved to malicious
        # commits; SHA pin is immutable).
        uses: gitleaks/gitleaks-action@ff98106e4c7b2bc287b24eaf42907196329070c7
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      # Un-06 REALITY CHECK: if `git push` fails after the Buttondown 2xx,
      # the archive commit stays only on the ephemeral runner. The next
      # cron starts from a clean checkout — no issue.md, no .published —
      # so the backfill scan finds nothing and the orchestrator runs
      # normally, issuing a DUPLICATE send. Backfill detect-only protects
      # the in-process partial-write case, NOT a failed push. A proper
      # remote-side guard is future work.
      - name: Push daily archive
        if: ${{ steps.commit.outputs.committed == 'true' }}
        run: |
          set -euo pipefail
          # Rebase onto remote to absorb any commits that landed between
          # checkout and now (rare on a single-writer cron branch, but cheap
          # insurance). Autostash preserves the just-made archive commit.
          git pull --rebase --autostash origin ${GITHUB_REF_NAME}
          if ! git push origin HEAD:${GITHUB_REF_NAME}; then
            echo "::error::Un-06 DIVERGENCE — Buttondown publish succeeded but git push failed. The archive commit is stranded on this runner; the next run starts from a clean checkout and will re-collect + re-publish, sending a DUPLICATE to subscribers. Operator must investigate before the next cron fires (e.g. retry push from local, or pause the workflow)."
            exit 1
          fi
